# `internal` 设计说明

`@internal` 的目标是把低层数值实现细节从公开表示包里抽出来。

当前它集中承载：

- `BigInt` 小工具
- 2/10 因子剥离
- 精度缩减时的舍入
- 十进制字符串预处理

这样做的价值是：

- `bin_float` 与 `decimal` 不需要重复写同类逻辑
- 舍入和规范化语义更容易保持一致

虽然这里的函数已经被文档化，但它仍然应该被视为实现辅助层，而不是对外承诺长期稳定的包接口。
