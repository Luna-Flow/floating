# `bench/decimal` 设计

## 职责

分离系数成本、上下文 `Decimal` 成本与 `DecimalChecked` 包装成本。

## 数据流

确定性十进制系数在计时外生成，并覆盖 9、34、128 与 512 位十进制精度。

## 不变量

工作精度足以保持精确算术，因此各层都以同一整数参考值校验。

## 副作用

只有显式包含的 skipped test 才计时并输出 artifact。

