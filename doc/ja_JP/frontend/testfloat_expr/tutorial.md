# `frontend/testfloat_expr` チュートリアル

まず同梱 vector を実行します。

```sh
just conformance smoke binary
```

`TestFloatSpec`、vector source、任意の `RunOptions` shard、document 実行の順です。function metadata は path から推測せず明示します。
