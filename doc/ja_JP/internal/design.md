# `internal` 設計ノート

`@internal` は低レベル数値処理を公開表現パッケージから分離するために存在します。

現在ここに集約されているのは:

- `BigInt` 小物補助
- 2 / 10 因子除去
- 精度縮小時の丸め
- 10 進文字列前処理

これにより `bin_float` と `decimal` で同種のロジックを重複させずに済みます。

文書化はされていますが、外部向けの安定契約として扱うべきではありません。
