# `internal`

Shared exact-rational, BigInt, parsing, normalization, and rounding helpers.

## Contract

This package is an implementation boundary, not a stable downstream contract.

## Maintainer Quick Start

```sh
sh tools/run_moon_clean_exec.sh test -p Luna-Flow/floating/internal --target native
```

## Detailed Documentation

The synchronized English, Simplified Chinese, and Japanese references live under
`doc/*/internal`. Read `api.md` for the generated surface, `tutorial.md` for the
shortest workflow, and `design.md` for invariants and trade-offs.
