# `internal/runner_cli`

Effect boundary for common conformance CLI options, files, diagnostics, and JSON.

## Contract

It keeps filesystem and rendering concerns out of pure frontends.

## Maintainer Quick Start

```sh
sh tools/run_moon_clean_exec.sh test -p Luna-Flow/floating/internal/runner_cli --target native
```

## Detailed Documentation

The synchronized English, Simplified Chinese, and Japanese references live under
`doc/*/internal/runner_cli`. Read `api.md` for the generated surface, `tutorial.md` for the
shortest workflow, and `design.md` for invariants and trade-offs.
