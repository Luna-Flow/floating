# `cli/testfloat_expr_cli`

Command adapter for Berkeley TestFloat vector files.

## Contract

Function, format, rounding, tininess, and sharding metadata remain explicit.

## Maintainer Quick Start

```sh
sh tools/run_moon_clean_exec.sh run --release --target native src/cli -- --backend testfloat --help
```

## Detailed Documentation

The synchronized English, Simplified Chinese, and Japanese references live under
`doc/*/cli/testfloat_expr_cli`. Read `api.md` for the generated surface, `tutorial.md` for the
shortest workflow, and `design.md` for invariants and trade-offs.
